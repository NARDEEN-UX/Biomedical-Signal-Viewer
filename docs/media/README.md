# Media Assets

Store all project visuals here.

## Structure

- `screenshots/` for static images used in the main README.
- `videos/` for short demos and walkthrough clips.

Recommended naming convention:

- `section-purpose.png` (e.g., `acoustic-doppler-sim.png`)
- `section-demo.mp4` (e.g., `market-demo.mp4`)
